apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: {{ .Values.namespaces.database }}
spec:
  selector:
    app: mongodb
  ports:
  - protocol: TCP
    port: {{ .Values.mongodb.port }}
    targetPort: {{ .Values.mongodb.port }}
  type: ClusterIP